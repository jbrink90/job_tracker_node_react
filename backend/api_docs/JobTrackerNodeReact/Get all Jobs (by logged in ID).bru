meta {
  name: Get all Jobs (by logged in ID)
  type: http
  seq: 4
}

get {
  url: {{api_url}}/jobs
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

tests {
  if (res.status === 401) {
    throw new Error("🔐 Token expired — refresh required");
  }
  
}
